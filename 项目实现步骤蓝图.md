---
blueprint_schema: tavernweave/blueprint/v1
project_id: airp
blueprint_id: BP-ROOT
title: AIRP 第一版实施蓝图
status: driver-approved
updated: 2026-09-19
---

# AIRP 第一版实施蓝图（single-blueprint，v4 同步版）

执行规则：一次只激活一个阶段；每阶段先明确输入/输出/改动边界/退出条件/证据；runtimePersistentBlueprintBudget=0；真实问题可开一层临时支线，关闭后回归父步骤；阶段退出后更新 NEXT.md 再进下一阶段；首版闭环后停在驾驶员验收，不自动滚入 Growth Tracks。

## 阶段 0 · 基座验证 Spikes（证据先行，不写产品代码）

- 输入：总设计案 v4；pi-ai 包文档与源码。
- 任务：
  1. pi-ai 独立可用性 spike：脱离 pi-coding-agent 作为 provider 层调用（OpenAI 兼容 + 至少一家非 OpenAI 系），验证流式/AbortSignal 取消/usage 元数据。不成立则退回自写 OpenAI 兼容层（预案已冻结）。
  2. 端点能力探测 spike：四级降级阶梯（tool calling/JSON mode/prompt+parse/禁用）探测调用设计 + 代表性端点验证。
  3. 依赖锁定落地：pnpm 锁文件入库，写下 pi-ai 升级流程（显式动作 + golden transcript 门槛）。
- 输出：`docs/spikes/阶段0-基座验证.md`（结论+命令+输出摘录）。
- 退出条件：两个 spike 有明确成立/不成立结论；锁定政策成文。
- 边界：不创建产品目录结构，不写 Core。

## 阶段 1 · Core 语义与组装管线设计

- 输入：总设计案 v4；阶段 0 结论。
- 任务：
  1. 撰写 `docs/core/prompt-组装四件套.md`（预算优先级表/状态选择策略/跨 provider token 估算/缓存感知组装：稳定前缀+变化后置+组装哈希）。
  2. 纯 TS 建模：角色卡（原版不可变+工作副本）、楼层树（swipe/编辑/回退/撤销回退=分支）、世界书双模语义、组装块管线（块可插拔，默认=四件套，扩展钩子预留）、StateOp（set/inc/push/unset 锚定 floorId）。
  3. 假模型/假工具适配器跑通最小循环（单测）。
- 退出条件：四件套文档经驾驶员过目；Core 单测通过；Core 不 import 进程/网络/存储 API（lint 强制）。
- 边界：无持久化、无 HTTP、无 UI。

## 阶段 2 · Runtime 持久化地基与生成生命周期

- 任务：
  1. 一卡一目录存储：卡空间物理独立；JSONL append-only 事件日志（patch-first）+ 每 N 楼快照 checkpoint；版本化迁移（迁移前自动备份）；一键导出 JSON。
  2. Session/Run/有序事件流；取消语义（AbortSignal 经端口入适配器）；kill -9 可恢复。
  3. **后端拥有生成生命周期**：Run 状态持久化，SSE 仅为视图；断线/刷新 reattach 恢复进行中输出。
  4. 服务器骨架：Hono，bind 127.0.0.1 + 启动令牌/来源校验；API key OS keychain（不可用则加密文件）。
- 退出条件：事件重放一致性测试；kill -9 恢复测试；reattach 恢复测试；迁移+备份演练；导出-重导往返。
- 边界：不接真实模型；前端仅 API 占位；SQLite 不建（保留为后续可选索引层）。

## 阶段 3 · 状态系统与后台管家

- 任务：通用状态 schema；事件溯源状态（快照+重放+遗忘语义：删分支=物理遗忘）；管家四级降级阶梯+能力探测；一致性协议（组装等前一楼）；摘要版本化懒重算（按 branchId 派生）；复盘合并卡级记忆；世界书双模（工具检索优先/关键词兜底）；token 消耗记录；管家仅经公开 ABI 工作（内置插件原则，ABI 形状随管家需求成型）。
- 退出条件：降级四级 fixture 测试；摘要失效-重算夹具；记忆召回评估套件（假适配器）；遗忘语义测试（删分支后状态不可见）。
- 边界：真实模型评估在阶段 5；ABI 对外文档在 Growth（L2）。

## 阶段 4 · 前端

- 任务：TanStack Virtual 楼层列表；分支 UI（swipe/编辑/回退/撤销回退）；运行时间线视图；状态面板；token 成本显示；调试日志导出（含隐私脱敏提示）；流式渲染（SSE+reattach）；输出 DOMPurify 消毒。
- 退出条件：万楼夹具冷启动 < 2s、滚动 60fps（trace 证据）；XSS 用例通过；断线重连 UI 恢复演示。
- 边界：不做主题系统、不做移动端适配。

## 阶段 5 · First Playable 闭环验收

- 任务：真实 ST 卡真实模型 e2e（导入→对话→swipe→回退→撤销回退→重开恢复）；记忆召回评估套件真实模型运行；性能预算复核（含**缓存命中率 ≥90%**：组装哈希追踪统计）；安全检查表；NEXT.md 更新。
- 退出条件：总设计案"验收证据"全部满足 → 停在驾驶员验收。
- 边界：不滚入任何 Growth Track（预设转译器为 Growth 首位，不在本蓝图）。

## 依赖顺序与临时支线

0 → 1 → 2 → 3 → 4 → 5。阶段 3/4 可部分并行，退出条件各自独立。任何阶段出现可观察失败：开一层临时支线（绑定父步骤、存证据、不扩大范围），关闭后回归。

## 停止条件

- 任一阶段退出条件无法满足且临时支线确认阻塞：停下并在 NEXT.md 记录阻塞，等待驾驶员裁决；
- 产品范围、宿主或许可证目标变化：先写变更提案，不静默重写已确认设计；
- 阶段 5 闭环完成：停在驾驶员验收，不自动滚入 Growth Tracks；
- 驾驶员要求暂停：保留四态账本与本蓝图状态，立即停。

## 下一道门

设计已批准。驾驶员说"按蓝图开跑第一版"，从阶段 0 开始执行。

## 当前活动阶段

阶段 0、1、2、3、4、5 已全部交付完毕并满足退出门禁。当前状态：**已完成全部阶段，停在驾驶员验收（driver-acceptance）**。

阶段 5 证据：真实 ST 卡片全生命周期测试通过；长程记忆召回（k 到 k+50 楼跨度）测试通过；性能预算（单次组装 < 300ms、前缀缓存命中率 ≥ 90%、万楼冷启动 < 2s）全达标；安全红线与强杀韧性全通过；全量测试 23 文件 97 用例全绿；三架构层独立探针全绿；报告见 `docs/验收/阶段5-完成报告.md`。
