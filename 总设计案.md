---
authority_schema: tavernweave/total-design/v1
project_id: airp
title: AIRP — 酒馆生态的记忆引擎
status: driver-approved-design
updated: 2026-09-19
next_gate: run-first-playable
---

# AIRP · 总设计案 v4（驾驶员已批准）

> 定位一句话：**酒馆生态的记忆引擎——导得进你所有的卡、预设和世界书，记得住你聊过的每一楼。永不商业化。**
> 本版经两轮第一性原理红队 + 四个参考项目（TauriTavern/梨园/dsh-tavern/Luker）分析 + 逐选型第一性原理推导，全部 13 项修订由驾驶员批准。

## 目标与非目标

**目标**：为 RP 社区提供本地运行的独立叙事应用，默认路径下——
1. 超长聊天（万楼级）流畅（解决酒馆卡顿）；
2. 角色长期记忆自动维护，用户零操作（解决记忆缺失）；
3. 消化酒馆生态资产（卡/世界书/预设/正则/MVU），不做生态的克隆而做超集。

**非目标（首版）**：卡内 JS/扩展执行、移动端、多设备同步、主题系统、群组聊天、商业化。

## 许可证与红线

- 许可证：**PolyForm Noncommercial 1.0.0**（个人/非商业自由使用、修改、分发；商业用途需单独授权）。第一性原理：MIT/Apache 允许商用违背意图；AGPL 不禁止商用且网络使用触发开源义务；NC 是意图的唯一落点。许可证只能收紧不能放松，首发即 NC。
- **clean-room 红线**：SillyTavern、TauriTavern、Luker 均为 AGPL-3.0，与 PolyForm NC 不兼容——其代码一行不抄，兼容实现全部基于文档化行为 + 自有 fixture。梨园（PolyForm NC）与 dsh-tavern（许可证未核实，按不可复用对待）同样只学概念。
- 依赖使用合法：pi-ai 为 MIT，作为依赖无冲突。
- T4 红线：沙箱方案落地前不执行卡内任意 JS。

## 项目类型与承载面

- 独立叙事应用（不依赖 SillyTavern 宿主）；本地 Node 服务器 + 浏览器前端（方案 B，driver override 已知安装摩擦）。
- 分发：`npx airp` 启动并自动开浏览器；一键安装器与桌面打包在 Growth Tracks。
- 技术栈：Node 24 + TypeScript strict + pnpm 单包；Hono；Vitest；Zod；React 18 + Vite + Zustand + TanStack Virtual + Tailwind；存储为一卡一目录 + JSONL（见 Core Spine 2）。

## 用户价值与核心循环

**楔子**：生态超集 + 非商业 + 事件溯源 + 可验证记忆。四参考项目各占一角，该象限为空。

**核心循环**：用户发话 → prompt 组装（四件套，见下）→ 模型流式生成（**后端拥有生成生命周期**：Run 持久化，SSE 仅为视图，断线/刷新可 reattach 恢复）→ 渲染为楼层并持久化 → 后台管家楼间异步完成状态更新（滚动摘要按 token 阈值触发）→ 下次组装使用新状态。

**管家一致性协议**：prompt 组装等待紧邻前一楼的管家完成；失败显式降级入时间线，绝不静默。

**管家降级阶梯**：tool calling → JSON mode → prompt 内嵌 JSON 解析重试 → 显式禁用并告知。首次运行自动探测端点能力定级；**世界书同为双模**：端点支持工具调用→设定集按需检索（模型主动查原文）；不支持→关键词注入兜底。

**成本治理**：管家/摘要频率可配置；摘要按 token 阈值触发；每次运行 token 消耗 UI 可见；缓存感知组装直接降低重复前缀成本。

## 四态决策账本

### 已确认（v3 全部 + v4 新增）

v3：1-21 项（独立叙事应用/社区痛点/方案B/从零/pi-ai仅provider层/全新项目/许可证/记忆c+a主干/后台管家/事件溯源/转译优先策略/T4红线/兼容报告/swipe编辑回退进首版/管家一致性/降级阶梯/成本治理/三件套先行/摘要懒重算/存储地基/记忆评估+安全红线/前端栈）。

v4 新增（驾驶员 2026-09-19 全部批准）：
22. 许可证 MIT → **PolyForm Noncommercial 1.0.0**；clean-room 红线维持并扩展至全部参考项目。
23. **存储变更**：一卡一目录 + JSONL 事件日志 + 快照 checkpoint；SQLite 降为可选派生索引层。
24. **世界书双模**（工具检索优先/关键词兜底，纳入降级阶梯）。
25. **遗忘与复盘**：删除分支=该分支状态事件物理遗忘；会话结束管家复盘合并为卡级记忆。
26. **生态兼容四层架构**（见下）+ prompt 组装改可插拔"组装块管线"（默认排序=四件套）+ 预设转译器列 Growth Track 首位。
27. **四件套**：三件套 + 缓存感知组装（稳定前缀/变化后置/组装哈希追踪）；缓存命中率 ≥90% 进性能验收。
28. 首版增补：**撤销回退**（恢复点随新生成/编辑失效）；**原版/工作版**（导入资产原版不可变，修改落工作副本）；**调试日志导出**（含隐私脱敏提示）。
29. **依赖锁定政策**：锁文件入库；pi-ai 升级=显式动作 + 跑通 golden transcript 方可合并。
30. **管家=内置插件原则**：自家功能只走公开 ABI，ABI 由管家这个第一用户验证。
31. **后端拥有生成生命周期 + 断线 reattach** 进首版 Runtime 要求。
32. **ABI 版本化**（abiVersion 声明+不兼容显式报错）+ 组装块钩子（扩展可注册组装块，挂于 L2）。
33. Growth 增补：分岔决策卡、意图化局部改稿、预设文件化手编、行动候选、剧本模式、一键安装器。

### 提案中 / 待决定
（无）

### 已否决
- 酒馆同层前端（解决不了卡顿）；全量 Pi SDK 集成（价值清单仅 provider 层通过）；高保真模拟 MVU（背别人包袱）；fork/贡献 TauriTavern 代替立项；纯静态 PWA 承载面（保留为极端退路）；首版桌面 exe（Growth）；MIT 许可证（v4 被 PolyForm NC 取代）；SQLite 主存储（v4 被一卡一目录+JSONL 取代，保留为可选索引）；世界书纯关键词触发（v4 被双模取代）；嵌入 ST 代码换兼容（AGPL 污染，clean-room 否决）。

## Core Spine

1. **RP Core（纯 TS，无进程无网络）**：角色卡（ST v2 导入，原版不可变+工作副本）、楼层树（swipe/编辑/回退=分支）、世界书、**组装块管线**（块可插拔，默认排序=四件套；扩展钩子预留）、状态与记忆领域语义、领域错误与策略。
2. **Runtime**：Session/Run/有序事件流；**一卡一目录**（卡空间物理独立，拷贝即迁移）+ JSONL append-only 事件日志（patch-first，天然免全量重写）+ 快照 checkpoint + 版本化迁移（迁移前自动备份）+ 一键导出 JSON；取消与断点恢复；**后端拥有生成生命周期**（Run 持久化、SSE reattach、刷新恢复）。
3. **ModelPort → pi-ai 适配器**：BYOK 多 provider；SDK 仅此端口之后；golden transcript + 锁文件防漂移。
4. **状态系统（事件溯源）**：schema 校验的结构化事实/变量；变更 append-only 锚定楼层 ID；任意楼层状态=最近快照+重放；回退/分支/遗忘天然正确。
5. **后台管家**：楼间异步状态更新与摘要；四级降级阶梯+能力探测；输出 schema 校验非法重试；复盘合并卡级记忆；**只通过公开 ABI 工作（内置插件原则）**。
6. **前端**：虚拟化消息列表 + 运行时间线 + 状态面板 + swipe/编辑/回退/撤销回退 + 成本可见 + 调试日志导出。

**核心算法四件套（先于实现成文）**：① prompt 预算优先级表与截断策略；② 状态→prompt 选择策略（recency+关键词命中+固定预算上限）；③ 跨 provider token 估算（映射表，无映射保守高估 20%）；④ **缓存感知组装**（稳定前缀、变化后置、组装哈希追踪命中率）。

**安全红线**：API key 加密存储（OS keychain 优先）；localhost 来源校验/启动令牌；模型输出渲染前消毒；不执行卡内 JS。

## 生态兼容四层架构（Growth Tracks 主干）

- **L1 声明式资产转译管线**：卡/世界书/预设/正则/MVU/宏子集 → 统一 IR → 兼容报告。预设条目转译为组装块；记忆三件套成为预设可引用的内置块类型（比酒馆预设更强）。
- **L2 自有扩展 ABI**：沙箱（iframe/worker）+ 显式能力授权 + 权限安装时明示；ABI 版本化；管家等内置功能只吃公开 ABI。
- **L3 ST-API shim**：top-N 社区扩展 API 表面调查 spike 后按数据定覆盖范围；不兼容者明确报告原因。
- **L4 STScript/宏引擎**：纯函数子集先行，命令式部分按 shim 同策略。

## First Playable / First Usable（冻结版 v4）

导入 ST v2 角色卡（人格+开场白+内嵌世界书双模子集）→ 用户对话，虚拟化楼层渲染 → 后端拥有的生成生命周期（断线 reattach）→ 后台管家按一致性协议完成状态更新与摘要 → 状态面板可见、token 消耗可见 → swipe/重新生成/编辑/回退/**撤销回退**（状态自动重放，摘要按分支懒重算）→ 全部数据持久化于卡目录，重开完整恢复 → 调试日志可导出。

**验收证据（全部满足才算闭环）**：
- 假适配器单测：核心循环、分支语义、状态重放、管家降级四级、组装预算截断；
- golden transcript：假/真适配器同一 Core 语义一致；
- 记忆召回评估套件（k 楼埋事实，k+50 楼验证），假适配器与真实模型各跑通；
- 性能预算：冷启动开万楼 < 2s；滚动 60fps；单次 prompt 组装 < 300ms；**缓存命中率 ≥90%**；
- 韧性：生成中断线 reattach 恢复；kill -9 恢复；迁移+备份演练；导出-重导往返；
- 真实卡真实模型端到端：导入→对话→swipe→回退→撤销回退→重开恢复，时间线可查；
- 安全：key 不明文落盘、无令牌跨源被拒、恶意输出被消毒。

## Growth Tracks

预设转译器（首位）→ 生态兼容层（L2 ABI → L3 shim spike → L4 宏）→ MVU schema 导入 + 兼容报告扩展 → embedding 记忆增强 → 手动编辑单条事实 → 追加提交多气泡 → Agent Profile → SubAgent → 世界书完整语义 → 深度模式（前台 agentic，含分岔决策卡）→ 意图化局部改稿 → 行动候选 → 剧本模式 → 一键安装器/桌面打包 → 状态栏自定义组件 API。

## Parking Lot

卡内 JS 执行（等 L2 沙箱）/ 移动端 / 多设备同步 / 主题系统 / 群组聊天 / 商业化 / 竞品观察（新参考项目不再逐一轮审，记此备查）。

## 稳定作品复用账本

- **TauriTavern**（AGPL-3.0）：仅参考。工作区抽象、工具调用即运行事件、提交边界、Profile 约束、显式失败、分层实证、分发工程。
- **梨园 Liyuan**（PolyForm NC 1.0.0）：仅参考。coding→RP 映射法、设定集按需检索、一卡一目录+明文存储、遗忘/复盘、分岔决策卡、提示词文件化分层；其 vendoring pi 内核是依赖漂移的活警示。
- **dsh-tavern**（许可证未核实，按不可复用对待）：仅参考。缓存感知组装（95% 命中）、正文/后台分离、撤销回退、原版/工作版、依赖锁定之痛、调试日志导出、行动候选、剧本模式。
- **Luker**（AGPL-3.0）：仅参考。记忆=内置插件、后端拥有生成生命周期+断线恢复、patch-first 持久化、ABI 版本化与钩子点。
- **pi-ai**（MIT）：改造为 ModelPort 适配器；阶段 0 spike 验证独立可用性，不成立退回自写 OpenAI 兼容层。
- 其余旧项目：拒绝（驾驶员确认无关）。

## 前端适配结论

independent-ui / suitable：浏览器前端（本地服务器托管）。万楼虚拟化+贡献者生态；非酒馆宿主，同层/嵌入不适用。driverOverride=false。桌面/移动端进 Growth 时重审。

## 验收账本

| 门 | 状态 |
|---|---|
| 自动化测试（单测/transcript/记忆评估/性能/韧性） | not-run |
| 真实宿主（真实卡真实模型 e2e） | pending |
| 驾驶员设计确认 | passed（2026-09-19，13 项修订全部批准） |
| 驾驶员首版验收 | pending |

## 选型第一性原理备查

不可逆决策深挖（许可证/事件溯源/clean-room 转译/TS 生态），难逆决策接口隔离（存储/基座/记忆机制/管家一致性），可逆决策速决（框架类）。每条决策配推翻条件，全部转化为蓝图 spike 或验收指标。原则：分析深度∝不可逆性；痛苦外包心脏自留；方向选可加性。

## 红队与参考项目记录

- 红队一轮：swipe 漏项/管家一致性谎言/成本翻倍/幻觉纠正/两算法空白/世界书低估。
- 红队二轮：结构化输出端点假设/摘要失效级联/状态选择与 token 估算/记忆可度量/日志工程地基/本地安全三面。
- 参考项目：TauriTavern（AGPL 税与全兼容维护税）→ 生态四层策略；梨园（pi 深度集成可行性+存储形态）→ JSONL 变更；dsh-tavern（缓存工程+依赖锁定之痛）→ 四件套+锁定政策；Luker（内置插件原则+生成生命周期）→ ABI 原则+reattach。
- 脑暴漏项：预设生态（驾驶员提问捕获）；非商业约束（驾驶员追加，许可证修订）。

## 下一道门

设计已由驾驶员批准（driver-approved-design）。驾驶员说"按蓝图开跑第一版"后进入蓝图阶段 0。本确认不授权：安装/卸载、密钥或付费 API、真实外部发布、Git 提交/推送、部署、修改全局规则。
