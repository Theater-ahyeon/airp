# 阶段 1 · Core 语义与组装管线设计完成报告

**完成日期**：2026-09-19  
**执行人**：AIRP 开发团队  
**状态**：**已全部达成退出条件（Passed）**

---

## 交付物清单与对应位置

1. **核心算法四件套设计规范**：
   - 路径：`docs/core/prompt-组装四件套.md`
   - 涵盖：
     - 预算优先级表与截断策略（P0~P5 梯度，原子丢弃保障）；
     - 状态→Prompt 选择策略（$Score(e) = 0.5P + 0.3M + 0.2R$ 动态加权筛选）；
     - 跨 Provider Token 估算器（离线保守高估 10%~20%，保障不超限）；
     - 缓存感知组装（静态前缀、缓变摘要、动态尾部顺序与组装哈希追踪，支撑 $\ge 90\%$ 缓存命中率）。

2. **纯 TS 领域模型（零进程/网络/存储 API 隔离）**：
   - `src/core/types/character.ts`：角色卡不可变原版（`readonly original`）与运行时工作副本（`workingCopy`）；
   - `src/core/types/floor-tree.ts`：楼层树分支结构、Swipe 多重分支、楼层原位编辑版本追溯、回退与撤销回退（物理遗忘语义支持）；
   - `src/core/types/worldbook.ts`：世界书双模（常驻 always / 关键词命中 keyword / 工具按需检索 tool_search）；
   - `src/core/types/state.ts`：结构化状态与事件溯源 StateOp（set/inc/push/unset）重放引擎。

3. **组装管线与算法实现**：
   - `src/core/pipeline/token-estimator.ts`：跨 Provider 字符分词安全估算；
   - `src/core/pipeline/state-selector.ts`：状态根据查询、相关度、时效与置顶状态计算评分并按预算截断；
   - `src/core/pipeline/assembly-pipeline.ts`：可插拔组装管线，严格按缓存友好顺序组装，自动剔除超限历史楼层与低优先级块，生成 Prefix Hash。

4. **假模型/假工具适配器与测试用例**：
   - `src/core/adapters/mock-model.ts`：基于 AsyncGenerator 的假模型流式输出与 Tool Call 模拟适配器；
   - `tests/core/core.test.ts`：覆盖上述全部 8 项领域契约与端到端模拟循环，**Vitest 8/8 全绿通过**。

5. **领域层绝对隔离校验器**：
   - `scripts/check-core-isolation.mjs`：自动化静态扫描，防止 `src/core` 引入任何 `node:fs`, `node:http`, `node:child_process`, `node:process` 等环境模块。
   - 运行结果：`PASS: Core isolation verified across 8 core files.`
   - `tsc -b`：**TypeScript strict 零错误编译通过**。

---

## 阶段 1 退出条件复核

- [x] 四件套文档撰写完成，已成文供驾驶员过目；
- [x] Core 纯 TS 领域模型全部就绪；
- [x] 假模型/假工具最小循环单测全绿；
- [x] Core 绝对不 import 进程/网络/存储 API（自动化静态脚本守护通过）；
- [x] Strict TS 构建通过。

**结论**：阶段 1 成功闭环，准予进入**阶段 2 · Runtime 持久化地基与生成生命周期**。
